# 测试与验证说明

当前主要通过示例计划进行功能验证。

所有回归示例应优先使用本地 HTML 夹具，不依赖真实外部网站。

## 建议验证项

- 基础页面打开与输入
- 变量替换
- 数据源读取
- 条件分支
- 循环执行
- 交互式执行器事件流
- 管理终端命令
- AI 终端 plan 创建、失败分析、补丁确认和报告生成
- 失败现场采集：失败截图、失败 HTML DOM、失败页面状态 JSON 和 DOM 摘要
- AI 调试修复闭环：debug prepare、propose debug fix、run debug plan、生成干净 patch
- 专项 AI 组件 schema 校验和 `output/ai/` 产物约束
- 真实 AI 模型连通性、结构化抽取、分类和流程级回归

## 当前验证方式

- 运行 `test-plans/basic/` 下的基础 plan
- 运行 `test-plans/data-driven/` 下的数据驱动 plan
- 运行 `test-plans/ai/controlled-text/` 下的专项 AI plan，覆盖连通性、结构化抽取和分类
- 运行 `test-plans/ai/text-processing/` 下的专项 AI plan，覆盖文本转换和摘要
- 检查 `output/ai/` 下有请求、响应、解析结果和 schema 校验产物
- 运行 `test-plans/regression/failure-capture/` 下的失败采集 plan，确认 `output/<run>/failure-screenshots/`、`failure-html/`、`failure-page-state/` 都存在
- 调用 `analyze_latest_run_failure`，确认 `dom_summaries` 能返回输入框、按钮、链接等关键元素和 selector 提示
- 调用 `propose_debug_fix`，确认 selector 超时能生成候选；应用候选后 `patch.diff` 只包含真实修复，不包含调试注入步骤
- 对 selector 修复同时验证两类场景：没有 `user_hint` 时只返回候选和歧义原因，不写入 `injected-plan/`；提供明确目标提示时才允许自动写入高置信候选
- 检查各 plan 包自己的 `output/` 日志结果

## 已跑过的关键回归

- 2026-05-10：运行 `test-plans/regression/failure-capture/plan.json`，确认失败后生成 `failure-screenshots/`、`failure-html/`、`failure-page-state/`，`analyze_latest_run_failure` 能从 HTML 中提取 `#username`、`#password`、`#captcha`、`#submit-btn` 等 DOM selector 提示。
- 2026-05-10：通过 `prepare_failure_debug_workspace` 创建 `output/debug/<run>/source-copy/` 和 `injected-plan/`，注入 `print`、`write type:variables`、截图和 HTML 后真实运行调试副本，确认调试产物留在 plan 包 `output/` 下。
- 2026-05-10：带明确提示 `系统账户 username 用户名 输入框` 调用 `propose_debug_fix apply=true`，生成只替换 `steps[2].selector` 为 `#username` 的干净 patch，并运行 `run_debug_plan` 验证通过。
- 2026-05-10：不提供目标提示时调用 `propose_debug_fix apply=true`，确认 selector 自动修复门禁拒绝写入，只返回候选列表和歧义原因。
- 2026-05-10：通过真实 `python .\main.py ai --thread ...` 背后的 LangChain/LangGraph agent 验证无提示 selector 修复会停在候选列表，不调用 `apply_debug_patch_after_approval`，不修改原始 plan。

## AI 测试配置

`test-plans/config.json` 当前包含用户提供的临时 OpenAI-compatible 测试服务，用于真实 AI 场景回归。不要在清理旧 AI 代码或文档时删除这段配置；如果密钥过期或需要更换，由用户更新或明确要求移除。

公开示例 `plans/config.json` 不放真实密钥。
