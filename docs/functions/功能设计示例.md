# 功能设计示例

一个“登录表单自动化”功能的设计可以分成四层：

1. 用一个 plan 包表示一个完整需求
2. 用 `plan.json` 作为唯一入口
3. 用 `sub-plans/` 拆分较长流程
4. 用 `resources/` 保存本需求独占夹具

这样做的好处是：

- 打开一个 plan 包就能理解这个需求
- 不同需求之间不会互相引用
- 运行输出固定落在本需求的 `output/`
