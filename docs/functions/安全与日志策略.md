# 安全与日志策略

## 当前日志策略

每次执行都会创建独立输出目录，并记录：

- `run.log`
- `events.jsonl`
- `result.json`

失败时还会尽量自动保存当前页面截图、HTML DOM 和页面状态 JSON。

## 当前建议

- 不要把敏感凭据硬编码进公共示例计划
- 调试时优先使用测试账号与测试数据
- 对需要长期保存的日志做脱敏处理

## 后续可增强

- 敏感字段掩码
- 更细的错误分类
- 失败自动截图、HTML DOM 和页面状态 JSON
- plan 包级报告
