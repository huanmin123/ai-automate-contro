# 核心功能设计

## 当前核心能力

- JSON 计划执行
- 多浏览器会话管理
- 多页面切换与控制
- 变量替换与共享
- 页面交互动作
- 页面控制动作
- 浏览器状态保存
- 数据提取动作
- 断言动作
- 条件分支
- 循环处理
- 外部数据源加载
- 文件读写
- 重试机制
- 失败现场采集
- 单 plan 结果输出

## 能力分组

### 计划层

- `variables`
- `steps`

### 动作层

- 浏览器管理
- 页面跳转与等待
- 多页面切换
- 输入与点击
- 页面控制
- 浏览器状态
- 数据提取
- 断言
- 控制流
- 数据读取
- 文件读写
- 网络事件等待
- 表格结构提取

### 组件合并原则

同族能力按参数结构收敛：

- `navigate`: 页面跳转、刷新、前进、后退
- `page`: 新开页面、切换页面、关闭页面
- `element`: 点击、悬停、填充、清空、选择、上传等元素操作
- `wait`: 时间、选择器、URL、文本、数量等待
- `extract`: 文本、值、属性、HTML、数量、表格提取
- `assert`: 选择器、文本、值、URL、数量断言
- `capture`: 截图、HTML、storage state 保存
- `read`: 通过 `type` 读取 `json`、`text`、`csv`
- `write`: 通过 `type` 写出 `json`、`text`、`csv`、`variables`

合并原则：

- 参数结构一致、只差序列化格式的能力合并到同一个组件。
- 参数生命周期不同的能力单独保留，例如 `open_browser`、`run_sub_plan`、`foreach`、`retry`、`wait_for_popup`、`wait_for_download`、AI 节点。
- 所有写入动作只能产出到当前 plan 包的 `output/`，并按组件进入固定分区。

## 设计目标

- 对业务站点保持低耦合
- 对流程编排保持高可组合性
- 对单个需求保持清晰边界

## 当前范围边界

当前阶段的主线仍是浏览器自动化内核，不扩展普通 plan 的电脑控制、系统命令、桌面窗口、进程管理和坐标级键鼠输入能力。

跨平台电脑控制会作为远期方向单独设计，未来如果推进，应通过稳定 plan schema 和 Windows/macOS backend adapter 落地，详见 [跨平台电脑控制远期设计](./跨平台电脑控制远期设计.md)。

## 最新补强

- 新窗口场景：通过 `wait_for_popup` 捕获 popup，并注册为命名页面，继续纳入统一多页面调度。
- 前后端联动场景：通过 `wait_for_network` + `type: request/response` 验证按钮点击、提交等动作是否真正触发目标接口。
- 数据表场景：通过 `extract` + `type: table` 直接把页面表格提取为二维数组或字典数组。
- 文件结果沉淀：通过 `write` + `type: csv` 把提取结果、执行中间结果写入 CSV，方便后续回归与人工校验。
- 多页面一致性修复：条件判断、重试等待、休眠等待统一改为基于当前活动页面执行，避免单页模型残留。
