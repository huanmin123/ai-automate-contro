# 对外快速使用指南

这份文档面向第一次使用项目的人。项目本质上是一个 Python + Playwright 的 JSON plan 自动化内核：你用 `plan.json` 描述浏览器、变量、断言、文件读写、控制流和受控 AI 步骤，执行器按步骤运行并把产物写回当前 plan 包的 `output/`。

如果你拿到的是已经打好的 `out\ai-automate-contro` 目录或 zip，优先看 [分发包使用指南](./分发包使用指南.md)。

AI 能力分两类：

- 普通 plan steps 里只能使用受控专项 `ai` action，例如连通性检查、抽取、分类、转换和摘要。
- 开放式对话能力只存在于 plan 级 AI 终端，用来创建、运行、调试和修复 plan。

## 1. 准备环境

推荐环境是 Windows + PowerShell 7 + 全局或用户级 Python 3.13。项目依赖声明支持 Python 3.11 及以上。

项目不要求在仓库根目录创建 `.venv`。如果某台机器上已经有 `.venv/`，它只是本地临时开发环境；换到另一台 Windows 机器时不会存在 `/Users/.../.venv` 这种 macOS 路径，也不应该依赖它。

macOS 本地开发不要使用系统自带 `/usr/bin/python3`，优先使用 Homebrew Python，并把依赖安装到用户级 site-packages。详细命令见 [安装指南](../develop/安装指南.md#macos-homebrew-python-安装约定)。

```powershell
Set-Location F:\python\keygen-openai-account

$PSVersionTable.PSVersion
Get-Command python
python --version
```

安装项目依赖：

```powershell
python -m pip install -e .
```

安装 `ripgrep`。AI 终端的文本搜索只支持跨平台 `rg`，不使用 Windows 内置搜索兜底。

```powershell
winget install --id BurntSushi.ripgrep.MSVC -e
rg --version
```

安装 Playwright Chromium：

```powershell
python -m playwright install chromium
```

如果 `rg --version` 失败，先重新打开 PowerShell 7 再验证。仍然失败时，请先安装 `ripgrep`，或让维护者在你确认后帮助执行上面的 `winget` 安装命令。

## 2. 本地自检

首次安装后建议先跑自检：

```powershell
python .\main.py self-check env
python .\main.py self-check ai-stream
python .\main.py self-check ai-terminal
python .\main.py self-check ai-tools
python .\main.py tool check
```

这些命令主要检查本地依赖、工具注册、AI 终端会话、图片附件和流式解析。除真实 plan 运行外，自检不会替你检查模型账号是否欠费，也不会隐藏模型服务返回的 503、协议不兼容或 schema 错误。

## 3. 认识 Plan 包

一个 plan 包代表一个独立自动化需求，主入口固定叫 `plan.json`。

```text
my-plan/
  plan.json
  config.json
  resources/
  sub-plans/
  output/
```

常见目录含义：

- `plan.json`: 最小可执行入口。
- `config.json`: 当前 plan 包的局部配置，优先级高于集合级配置。
- `resources/`: HTML、JSON、CSV、TXT 等输入资源。
- `sub-plans/`: 子计划，只能由同包主计划通过 `run_sub_plan` 调用。
- `output/`: 每次运行的日志、截图、JSON、变量、AI 调试产物和失败现场。

运行产物必须留在当前 plan 包的 `output/`。不要把截图、Cookie、storage state、日志、真实密钥或 `.keygen/` 会话状态提交到 Git。

## 4. 运行示例 Plan

先校验公开示例：

```powershell
python .\main.py plan validate --file .\plans\minimal-browser-plan\plan.json
```

再运行：

```powershell
python .\main.py plan run --file .\plans\minimal-browser-plan\plan.json --run-name demo-minimal-browser
```

运行完成后，到对应 plan 包的 `output/` 查看本次 run 目录。常见文件包括 `state.json`、`report.md`、`result.json`、`run.log` 和 `events.jsonl`。

## 5. 使用 AI 客户端

```powershell
python .\main.py
```

默认入口会打开 Textual AI 客户端。直接描述需求即可，客户端会把用户消息、AI 回复、工具进度和审批状态分块显示，不再有 `plan>` 或 `ai>` 模式切换。

确定性 plan 操作继续使用一次性 CLI 子命令：

```powershell
python .\main.py plan validate --file .\plans\minimal-browser-plan\plan.json
python .\main.py plan run --file .\plans\minimal-browser-plan\plan.json --run-name demo-minimal-browser
python .\main.py tool check
```

AI 客户端适合自然语言创建、运行、调试和修复 plan；脚本化、回归和打包验证使用上面的 CLI 子命令。

## 6. 配置 AI 服务

AI 服务配置来自当前运行根的 `default_ai_config_dir/config.json`，或当前 plan 包自己的 `config.json`。发行包默认使用 `plans/config.json`。如果文件中包含真实服务和 key，请只保留在本机，不要公开分享。

`plan.config` 控制 `handbook_path`、`plan_roots` 和 `default_ai_config_dir`。发行包会带 `plan.config`，默认 plan 集合是 exe 同目录下的 `plans/`。AI 终端创建 plan 时，如果用户没有明确给目录，会默认落到当前运行根的第一个 `plan_roots`，发行包通常是 `out\ai-automate-contro\plans\<name>\`。

示例结构如下：

```json
{
  "ai_services": {
    "default": {
      "base_url": "https://your-openai-compatible-endpoint/v1",
      "model": "your-model",
      "api_key": "sk-your-key"
    }
  }
}
```

项目按 OpenAI-compatible 协议请求模型服务。不会把自定义上下文、事件、thread metadata、附件 metadata 或项目内部字段塞进模型请求体。模型厂商返回欠费、503、协议不兼容或输出不符合 schema 时，项目会直接报告错误，不做手动降级、手动重试或改格式兜底。SDK 或 LangChain 内置的传输重试可以保留。

## 7. 使用专项 AI Action

普通 plan steps 里只允许受控专项 `ai` action，并通过 `type` 表示任务类型：

- `connectivity`
- `extract_data`
- `classify_text`
- `transform_data`
- `summarize_text`

专项 AI 必须有固定输入、固定输出 schema 和固定系统提示词。请求摘要、原始响应、解析结果和 schema 校验信息会写入当前 plan 包的 `output/ai/`，便于排查。

## 8. 使用 AI 终端

AI 终端适合让模型帮你围绕某个 plan 做创建、调试、运行、失败分析和补丁建议。

```powershell
python .\main.py
# 进入后输入：ai

python .\main.py ai
python .\main.py ai --thread demo-thread
```

常用命令：

```text
/status
/new
/sessions 20
/resume 1
/compress handoff-before-long-debug
```

交互输入行支持 `/` 命令提示。输入 `/` 会显示常用命令候选；继续输入前缀会实时过滤，例如 `/s` 会过滤到 `/sessions`、`/status`。需要键盘选择时按 `Up` / `Down` 切换候选，按 `Tab` 或 `Enter` 补全命令；带参数的命令继续按 `Enter` 发送。

发送自然语言后，AI 终端会用灰色输入块回显你的消息，AI 回复会直接在普通背景流式输出，不再带发言人前缀；当前输入行是灰底无边框多行 composer，内容换行或窗口变窄后高度会在有限范围内增长。AI 调用工具时会显示工具进度块，并在工具完成后原地更新为完成或失败状态，避免刷屏；系统输出、审批和错误会分别显示为独立块。输入 `/check` 可发送一次真实模型诊断请求，输入 `/details` 可切换完整工具细节。AI 正在回复或运行工具时，你继续发送的普通消息会进入队列；底部状态会显示服务、thread、当前 plan、debug workspace、最近 output、运行状态、排队数量、审批状态和待发送图片。模型服务报错会显示 service、model、base_url 和上游错误。AI 返回的 Markdown 只由 Textual 客户端展示层渲染；这个渲染只影响屏幕显示，不会改变模型请求、checkpoint、压缩归档或会话原文。需要留存当前屏幕对话时，用 `/export [path]` 导出 Markdown；只保存最近一条 AI 回复时，用 `/copy-last [path]`；清空当前屏幕用 `/clear`。

输入多行需求时，普通 `Enter` 始终用于发送当前消息；需要手动续行时，在行尾输入 `\` 后按 `Enter`；多行文本从剪贴板粘贴时会保留换行。单独按 `Esc` 仍用于中断当前 AI/plan，已有队列时会把排队内容作为下一轮介入消息优先处理。

让 AI 为真实网站创建 plan 时，请尽量给出 URL、目标流程和必要的测试数据。AI 终端会先用受控网页探测工具真实打开页面，读取标题、最终 URL、表单、输入框、按钮、链接、表格和验证信号；涉及登录、验证码、后台菜单或动态页面时，会继续创建并运行 `open_browser.headed=true` 的探索 plan，把流程先跑通再写最终 plan。需要你介入时，自动化会停在 `manual_confirm`，你应在同一个 Playwright 浏览器窗口里操作，而不是另开自己的浏览器。AI 可以在你授权下正常填写登录字段、点击、提交表单，或输入你提供的一次性验证码/二次验证信息；如果尝试不过、缺少短信/邮箱/设备确认或风险不明确，再交给你处理。它不会只按文字猜 selector，也不会把真实账号密码硬编码进最终 plan。

图片输入的主路径是“复制图片后继续说话”。截图或复制图片后，在 AI 终端当前输入行按 `Ctrl+V`；如果剪贴板是图片或图片文件，客户端会插入 `[图片 #1]` 图片标记，然后直接接着输入文字：

```text
[图片 #1] 帮我看这张页面为什么登录失败
```

删除图片时，不需要手动改附件列表。把光标放在 `[图片 #n]` 后面按 `Backspace`，或放在标记前面按 `Delete`，终端会整块删除图片标记、移除对应附件，并自动重编号剩余图片。光标不会停在 `[图片 #n]` 中间，输入文字时也会自动跳过图片标记，避免占位符被写坏。

如果图片已经保存成文件，也可以用 `/image .\screenshots\page.png` 作为兜底。图片附件会先落到 `.keygen/ai-terminal-sessions/<thread>/attachments/`，发送前转换为标准 `image_url` data URL。发送成功后清空 pending 图片；如果模型服务报错，附件会保留，方便你修复服务问题后重试。不要把 base64 图片内容写进源码、plan 或文档。

## 9. 上下文与会话恢复

AI 终端会把 checkpoint 写到 `.keygen/ai-terminal-checkpoints.sqlite`，会话摘要写到 `.keygen/ai-terminal-sessions/index.json`。关闭终端后，可以用同一个 thread 恢复：

```powershell
python .\main.py ai --thread demo-thread
```

也可以进入终端后查询和恢复：

```text
/sessions 20
/resume demo-thread
```

长会话按 128k token 上下文标准估算，在约 64k tokens 时自动压缩，并保留约 32k tokens 的近期上下文。完整消息会归档到：

```text
.keygen/ai-terminal-sessions/<thread>/compressions/
```

模型上下文里只保留摘要和归档路径，不会把完整 `run.log`、`events.jsonl`、`commands.jsonl` 或大型产物一股脑塞进去。需要排查时，AI 终端会先看结构和路径，再用 `rg` 定位关键词，最后读取必要的小范围片段。

## 10. 调试和修复 Plan

AI 调试修复必须先创建隔离调试工作区，再把最小补丁应用回原始 plan：

```powershell
python .\main.py plan debug-create --file .\plans\minimal-browser-plan\plan.json --name debug-login
```

典型调试工作区结构：

```text
output/debug/<run>/
  source-copy/
  injected-plan/
  notes.md
  report.md
  patch.diff
```

AI 或人工只能先修改 `injected-plan/`、`notes.md` 或 `report.md`。应用到原始 plan 前必须生成补丁并经过用户确认。AI 终端请求应用补丁时会进入 human-in-the-loop 审批，只有你输入 `/approve` 才会真正应用；`/reject <reason>` 会拒绝。

## 11. 常见问题

`rg` 找不到：

```powershell
winget install --id BurntSushi.ripgrep.MSVC -e
rg --version
```

浏览器启动失败：

```powershell
python -m playwright install chromium
python .\main.py self-check env
```

模型服务报 503、欠费、鉴权失败或 schema 不匹配：

先运行一次真实诊断：

```powershell
python .\main.py ai check --json
```

检查你的 `base_url`、`model`、`api_key` 和模型服务状态。项目会直接显示这类错误，不会替你切换模型、伪造协议或吞掉异常。

AI 终端回复很慢：

先看模型服务状态、网络和当前 AI 配置目录 `config.json` 里的 `base_url`、`model`、`api_key`。交互回合是前台等待并流式输出；如果不想继续等，可以按 `Ctrl+C` 中断当前等待。

## 12. 上线前检查

交付前建议至少跑：

```powershell
python -m py_compile .\main.py (Get-ChildItem .\src -Recurse -Filter *.py | ForEach-Object { $_.FullName })
python .\main.py self-check env
python .\main.py self-check ai-terminal
python .\main.py self-check ai-tools
python .\main.py tool check
python .\main.py plan validate --file .\plans\minimal-browser-plan\plan.json
python .\main.py plan run --file .\plans\minimal-browser-plan\plan.json --run-name release-smoke-minimal
git diff --check
git ls-files | rg '(^|/)(output|\.keygen|__pycache__)/|\.pyc$|\.egg-info(/|$)'
```

最后一条命令应该没有输出；如果有输出，说明 Git 里已经跟踪了不应该提交的本地产物，需要先从索引中移除。

需要分发可执行文件时，参考 [打包发布指南](./打包发布指南.md)。当前支持在 Windows 本机打 Windows 发行目录和 zip，在 Apple Silicon macOS 本机打 macOS 发行目录和 zip，统一输出到 `out/`。
