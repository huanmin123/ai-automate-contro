# 问题修复指导

遇到问题时，按下面顺序处理：

1. 先看 `output/` 里的 `run.log` 和 `events.jsonl`
2. 判断是选择器问题、等待问题、数据问题还是调度问题
3. 能用示例计划复现的，先最小化复现
4. 修复后补到 `bug/` 目录
5. 如果问题暴露出架构缺口，再补 `docs/plans/` 或 `refactors/`

## 常见问题类型

- 选择器不稳定
- 页面未完全加载
- 变量未定义
- 并发资源冲突
- 外部数据格式不符合预期
