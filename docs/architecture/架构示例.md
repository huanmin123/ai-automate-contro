# 架构示例

一个典型场景：

1. `suite` 文件定义三个项目
2. 每个项目引用自己的 `plan`
3. `suite` 决定是顺序还是并发
4. `plan` 内部通过动作组件驱动浏览器
5. 运行日志写入 `output/`

这意味着“项目级组织”和“页面级动作”已经分层：

- `suite` 负责项目级调度
- `plan` 负责单场景执行
- `action` 负责最小动作原子化
