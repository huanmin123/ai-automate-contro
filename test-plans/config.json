{
  "description": "Shared configuration for project test plans. Plan-local config.json can override these values.",
  "ai_services": {
    "default": {
      "provider": "openai-compatible",
      "api": "chat_completions",
      "base_url": "https://vsllm.com/v1",
      "model": "gpt-5.5",
      "stream": true,
      "timeout_seconds": 90,
      "strict_schema": true,
      "response_format": "json_schema",
      "note": "Temporary AI test service for real regression. Set AIC_TEST_API_KEY in the local shell; never commit inline keys.",
      "api_key_env": "AIC_TEST_API_KEY"
    }
  }
}
