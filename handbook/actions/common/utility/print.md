# print

## 用途

在控制台打印一段消息。

## 必填字段

- `action`: 固定写成 `print`
- `message`: 要输出的内容

## 示例

```json
{
  "action": "print",
  "message": "当前提取到的邮箱是 {{email}}"
}
```

## 什么时候用

- 调试计划流程
- 给人工操作步骤加提示
- 输出关键变量

## 注意事项

- `message` 支持变量替换。
- 它不影响浏览器页面，只在控制台输出文本。
