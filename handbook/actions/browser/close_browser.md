# close_browser

## 用途

关闭一个已经打开的浏览器会话。

## 必填字段

- `action`: 固定写成 `close_browser`
- `browser`: 要关闭的浏览器会话名称

## 示例

```json
{
  "action": "close_browser",
  "browser": "main"
}
```

## 什么时候用

- 你想在流程中提前关闭某个浏览器
- 多浏览器流程结束后，只保留指定窗口

## 注意事项

- 如果引用了不存在的会话名，执行器会报错。
- 即使你不手动写这个动作，计划结束时执行器也会自动清理所有会话。
